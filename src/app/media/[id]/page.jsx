import React from "react";
import Media from "./Media";

export const metadata = {
  title: "LineOne-Media",
  description: "Media",
};

const page = () => {
  return (
    <>
      <Media />
    </>
  );
};

export default page;

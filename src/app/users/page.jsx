import React from 'react'
import Users from './Users'

export const metadata = {
  title: "Users",
  description: "Lets connect",
};

const page = () => {
  return (
    <Users/>
  )
}

export default page
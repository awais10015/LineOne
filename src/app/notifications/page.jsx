import React from 'react'
import Notifications from "./Notifications"

export const metadata = {
  title: "LineOne-Notifications",
  description: "Notifications",
};

const page = () => {
  return (
   <Notifications/>
  )
}

export default page